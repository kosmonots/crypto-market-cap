{% extends 'default/layout/base.html.twig' %}
{% block body %}
    <style>
        .not-found{
            margin: 5em 0 0 0;
            text-align: center;
        }
        .page-margin{
            margin: 10em 0 25em 0;
        }
    </style>
    <section id="item-featured-search">
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <h3 class="category-page-title">Error 403</h3>
                </div>
            </div>
            <div class="animated fadeIn">
            </div>
        </div>
    </section>
    <div class="row">
        <div class="page-margin">
        <h1 class="not-found">Naughty! This is Forbidden.</h1>

        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            {# ... #}
        {% endif %}

        <p class="not-found">
            You don't have permission to access the requested page. Please contact admin for further assistance.
            <a href="{{ path('homepage') }}">return to the homepage</a>.
        </p>
        </div>
    </div>
{% endblock %}